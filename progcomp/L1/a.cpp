#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define lng long long int

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    vector<int> graph;
    int n;
    cin >> n;
    graph.resize(n);
    for(int i = 0; i < n; i++){
        cin >> graph[i];
    }
    for(int i = 0; i < n; i++){
        vector<int> mark;
        mark.resize(n);
        
    }

    
}